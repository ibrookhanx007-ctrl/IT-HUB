import type { PortfolioItem } from "@/types";

// No case studies yet. The portfolio page and grid already handle an
// empty list honestly (see emptyStateMessage in src/content/pages.ts)
// rather than showing placeholder projects — add real, consented case
// studies here as they become available.
export const portfolioItems: PortfolioItem[] = [];
